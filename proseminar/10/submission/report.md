# Exercise Sheet 10

**Team:** Marco Fröhlich

# Task 1
For smaller problem sizes the implementations for `Monte Carlo Pi` and `Matrix Multiplication` work. But not for the problem sizes in question from the task description.
To be more precise the `montecarlo.chpl` program terminated for $10^6$ points and 12 threads in less than 3 seconds. For $10^9$ points I canceled the execution after 10 minutes and no result.

For the `matrix_mul.chpl` the similar things happened.

I probably made some mistakes on installation/compilation but without errors debugging is more or less impossible.

I hope I never have to use `Chapel` again, I will now go and touch some grass and therefor omit making any measurements.

# Task 2
This task was skipped after the struggles with Task 1.